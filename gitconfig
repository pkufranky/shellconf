[core]
	autocrlf = false
[user]
	name = "Ping Yin"
	email = ping@adsnexus.com
[merge]
	summary = true
	tool = vimdiff
[diff]
	renames = copy
	wordregex = "[[:alnum:]_]+|[^[:space:]]"
[i18n]
	logoutputencoding = UTF-8
[color]
	diff = auto
	status = true
	branch = auto
	interactive = auto;
	ui = auto;
[showbranch]
    default = --topo-order
    default = heads/*
[status]
	submodulesummary = -1
[format]
	numbered = auto
[push]
	default = tracking

[sendemail]
	smtpencryption = tls
	smtpserver = smtp.gmail.com
	smtpuser = pkufranky@gmail.com
	smtpserverport = 587

[alias]
	sed = !git ls-files --stage | grep ^100 | awk '{print $4}' | xargs -t sed
	slg = shortlog --pretty='format:%h %s'
